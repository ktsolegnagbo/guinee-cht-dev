{
    "context": {
        "expression": "user.role === 'reco' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInYears(contact) >= 14 && ageInYears(contact) < 80 && contact.sex === 'male'",
        "person": true,
        "place": false
    },
    "icon": "family",
    "title": [
      {
        "locale": "en",
        "content": "Man FP"
      },
      {
        "locale": "fr",
        "content": "PF Masculin"
      }
    ]
}
