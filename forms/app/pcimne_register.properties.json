{
    "context": {
        "expression": "user.role === 'reco' && user._id !== contact._id && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInDays(contact) >= 60 && ageInDays(contact) < 1825",
        "person": true,
        "place": false
    },
    "icon": "child-50x",
    "title": [
      {
        "locale": "en",
        "content": "Registre PCIMNE"
      },
      {
        "locale": "fr",
        "content": "Régistre PCIMNE"
      }
    ]
}