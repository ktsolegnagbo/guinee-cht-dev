{
  "context": {
    "expression": "user.role === 'reco' && user._id !== contact._id && !summary.isPregnant && summary.alive && summary.is_family_person && !summary.muted && contact.type === 'person' && ageInYears(contact) >= 10 && ageInYears(contact) <= 60 && contact.sex === 'female'",
    "person": false,
    "place": false
  },
  "icon": "people-family",
  "title": [
    {
      "locale": "en",
      "content": "Family planning renew"
    },
    {
      "locale": "fr",
      "content": "Renouvellement PF"
    }
  ]
}